apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alert-event-handler.fullname" . }}-test-connection"
  labels:
    {{- include "alert-event-handler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alert-event-handler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
