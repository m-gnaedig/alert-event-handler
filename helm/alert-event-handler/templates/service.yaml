apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-event-handler.fullname" . }}
  labels:
    {{- include "alert-event-handler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alert-event-handler.selectorLabels" . | nindent 4 }}
